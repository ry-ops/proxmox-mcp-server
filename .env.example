# Proxmox MCP Server Environment Configuration

# Proxmox host URL (required)
PROXMOX_HOST=https://10.88.140.151:8006

# API Token authentication (recommended)
PROXMOX_TOKEN_ID=root@pam!cortex-automation
PROXMOX_TOKEN_SECRET=your-token-secret-here

# Alternative: Username/password authentication
# PROXMOX_USER=root@pam
# PROXMOX_PASSWORD=your-password-here

# SSL verification (set to false for self-signed certs)
PROXMOX_VERIFY_SSL=false

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Request timeout in seconds
# PROXMOX_TIMEOUT=30
